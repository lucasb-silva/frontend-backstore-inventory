import { useNavigate } from "react-router-dom"
import { Api } from "../api/api"
import { toast } from "react-toastify"
import Form from "../components/Form/Form"

// Página Create
export default function Create() {
  // Retornando o componente <Form />
  return <div> <Form /> </div>
}